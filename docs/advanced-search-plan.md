# 進階搜尋與篩選功能開發計畫

## 當前狀態 (2026-05-01)
- [x] **階段一：介面架構優化 (UI/UX Refinement)**
    - 實作「煙囪式」展開面板。
    - 實作極限垂直壓縮佈局（h-8 按鈕、微型標題）。
    - 移除所有 X 按鈕，改用「再次點擊即重置」邏輯。
    - 實作「競爭/合作」置頂導航切換。
- [x] **階段二：國際化與狀態持久化**
    - 補齊中英文翻譯（含 Comp./Coop./Lt./Md./Hv. 等縮寫優化）。
    - 實作 `localStorage` 狀態記憶。
- [ ] **階段三：搜尋引擎接線 (Logic Integration)**
    - 將 `searchFilters` 注入 `getSearchResults` 與 `getRecommendations`。
    - 實作 BGG 機制過濾（如合作遊戲 ID 2023 識別）。

## 核心設計規範
- **極簡化視覺**：移除區塊邊框與背景，改用 `border-b` 分隔線。
- **高資訊密度**：針對手機直立螢幕優化，按鈕高度維持在 32px (h-8)。
- **無標題化嘗試**：類型切換區塊不使用標題，靠圖示與位置暗示功能。

## 待辦事項 (階段三預告)
1. 在 `sortStrategies.ts` 中新增過濾邏輯。
2. 串接 `playerCount` 到全域推薦邏輯（考慮與右下角開局設定同步）。
3. 實作「Best Only」的加權過濾。

---

## 實作細節紀錄

### 介面設計
*   **Chimney 面板**：右側面板採用 `flex-col`，展開時高度為 `h-full`，內容由下而上堆疊。
*   **收合按鈕**：固定在篩選區與開局控制項之間，作為視覺錨點。
*   **單位翻譯**：時長使用 `{m}分` / `{m}m` 格式。

### 數據連動 (預計)
*   **重度轉換**：BGG 1.0~2.0 -> Light, 2.0~3.5 -> Mid, 3.5+ -> Heavy。
*   **類型過濾**：檢查 `Mechanisms` 是否包含 "Cooperative Game"。
