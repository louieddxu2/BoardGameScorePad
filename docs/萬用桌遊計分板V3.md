記錄一下待實作的功能
[[萬用桌遊計分板已處理項目]]

編輯項目的介面其實不需要儲存按鈕置底，在右上角就可以了，一樣保留X



//文字框不支援輸入幾.幾？

//查表也可以分項查表嗎……
乘積輸入要支援更多乘積嗎？
分項加總如果確定項目的類別，是也可以像乘積輸入那樣？輸入羊幾隻，懷孕的羊幾隻，但主要的目標只是避免換列書寫，似乎有一點多餘感
這樣一想，直接提供+1，+3，+5的按鈕還比較快？按鍵次數基本可以降到3次內

//乘積輸入也可以分項加總……每項是兩數乘積，但UI不太好設計
美麗新世界可能需要這個設計
森森不息？計分超煩瑣
多米諾王國其實也需要（嘆）

//說來乘積輸入是否應把後項預設為1？會填寫就表示個數不為0吧
核心問題變成分項加總到底要不要變成獨立的設計（嘆），按鈕輸入可以分項，乘積輸入也可以分項，恐怕查表輸入也能分項（只是就沒有空間印說明文字了）

//項目框下方的說明也要自動換行？
//滿搞笑的截圖裡會換行，這好像又表示截圖跟實際畫面的比例不太一樣了，希望不會很容易有bug（嘆）

//文字框右邊的數字意義，對看到圖片的人來說比較不明確
//截圖時可以取消不印？
用一個大括號來示意加總？（但有點過於佔版面）

//新增項目時寬度沒有對齊，應該也要自動

//項目設定多色……？但好像很醜陋，如果沒有使用整個文字框來顯示的話

//原版本已知bug：
在網頁瀏覽下，點按姓名編輯不會正確地把姓名欄往上推，但PWA的版本會

V2：
//截圖項目名稱也要換行
實際上是用V2在測試，因為其有更新檔案儲存結構，品質會較好
[[Vibe coding的原則]]

//V2已知缺陷：
乘積輸入後項預設為1，但卻不會取代
退出時的訊息：記分板->計分板
UI介面沒有內建（嘆）
一般字體內建以及拋棄連網的中文字型
//皆已修復待V3一同修復


//正在實作V3對應圖片效果，上下的拖曳介面支援分開滑動。這個不用了

//V3已知bug：ios截圖不支援，因為不能異步截圖，應已修復

V3
其實分享給朋友的訊息也可以再完整一些吧，包含網頁連結？

//發現新遊戲很惱人，始終都會出現

在內建超過100筆時，考慮目錄的讀取量（其實超過100筆後，應該就不會有人用滑的來找遊戲了吧）
如果設定中英雙名稱也有好處，就是英語系的跳英文，中文系的跳中文，但搜尋時支援中英搜尋

自建計分板設定100筆為上限，可以弄個彩蛋之類的（X

分項累加也要可以「只呈現分項」

//清單列表要記錄當前被選中的按鈕，原本的版本有這個功能？但呈現方式要再想想，可能不適合用點點佔版面。
原來右下角會顯示標籤，但我沒有好好利用（嘆）
那可能傾向把這個需求移動到V3裡面來處理吧，跟介面重構有點關聯（二維選單）

根據玩家人數、點按時的選擇（擴充？），跳出不同的項目？
有些項目會省略，有些項目裡的選項會不一樣
//其中一個選擇是「全編輯介面」
//拍照計分紙來生成計分板……？
函式的函式（？），點按後要先點選對應的計分項目（會存放起來到左邊的項目格裡），然後才是計分。
不過這個功能會有要輸入幾次的疑慮（只需要輸入一次），如果弄到一半又想修改也很怪。但強迫使用者退回目錄才能改也有點怪


新增項目鈕應該從左邊多出來，而非多在右邊

新增白底的版本嗎……或是任何底色的版本可以嗎，有點異想天開
至少有個白底的吧

//考慮到要搭配實體計分紙的前提，可能就要微調一下了，還是支援自動運算的設置
其實就是把現在的運算改用其他項目的數值來輸入吧

//背景圖的邊框還是要選進來，輸入介面時不用呈現，但最終截圖要呈現

//預覽截圖時應該要能縮放圖片本身讓使用者檢視

//設定區域的項目格，應該被點擊時就只會亮起該區域的框，而非整個方格，並且整個方格也不再都視為點擊區域

//分項累加可以放在乘積運算的下方，跟基本運算的下方，但只有基本運算下方支援按鈕輸入

//平手的情況分成三種數字：
只看數字的話排名第幾
看玩家人數的話排名第幾
有多少玩家與自己數字相同
從這三個量就能窮舉所有平手情況
雖然現在實現的方式有點數學基礎就是了（嘆）

說明文不需要提到雲端，畢竟似乎也沒那麼穩定，要再想想怎麼呈現

按下載不應該需要重整雲端介面

背景圖下的計分方格應該有某些設定上的錯誤，會導致邊緣黑框，但這只在寬度相對較小時才會出問題

//Json 儲存預設玩家人數，進入時會優先顯示上次的人數

//自動的變數選擇玩家人數時，不應有取值模式的選項

//自動的公式框要能夠往右拖曳看到後面的內容，問題應該是因為確認公式的按鈕被放在文字框裡面了

//設定第二條公式後，似乎就會計算錯誤，沒有正確解讀兩個公式

//計分板預覽公式的介面一樣要能拖曳滑動查看公式，原本排版有誤變成往右將結果推出畫面外了

？用圖片建立時也要自動看有沒有網路上傳圖片備份

//乘以幾倍的輸入介面右邊的說明文字也要寫反過來的×c

在按鈕輸入狀態下也提供切換回數字鍵的選擇，可能是在左下角多一個三角形的按鈕吧
或是在此區域內從左往右大幅滑動

要有能夠取消背景圖的選擇？或者說只要有圖就要問使用者要不要載入

//使用者要能看出當下是否已連網

//虛擬添加對於白色的按鈕看不出效果，連數值也看不到

//範圍查表的右下角沒有正確顯示數量，只印了單位

//資料管理匯出介面的全選應該在右邊，並且應該更加醒目
而且資料管理下半部似乎有很大的空白區域？

//項目底色兩種灰階讓人能清楚看到區別

//角色功能本質上就是按鈕清單的文字版（嘆）
原本說不想做的功能可能還是變成要做了嗎？

//項目屬性按鈕待整合介面，目前仍太亂，而且不好參透。

接收bgstate 匯入檔案？這樣就有使用者的ID、地點等等的數據

二維查表？都有sumproduct 了應該不到難
可以用按鈕多欄來實現，某些按鈕設定成欄列說明就好
或是在裡面設定欄列說明
//分數格裡顯示選單的按鈕顏色（嘆）
還可以設定僅顯示按鈕（不顯示數字），就變成文字選單了

//分項累加輸入時也有預覽效果？

//先算個數再填分數，因為個數是要詢問（計算的），會有計數等待其他玩家延遲的問題，而分數是查表一眼看出來的（你是聽到誰先算出了數量，才會填寫乘上分數）

//好像實作了index DB 了，待檢查具體效果

//有了進程記錄的情況還是會觸發畫面閃爍

？備份訊息過多，應該要有高度限制（5則）

？是否與雲端相同的記憶是在本機端，可能耐不太住版本更新，但或許無妨。

？按下開始後要等待雲端上傳才能使用的意思？有點頓，應該背景上傳就好，不應影響使用者體驗，已測試斷網情況下是正常開啟的

？還沒有能瀏覽遊戲記錄雲端硬碟的介面，可能一樣要包含進行中、垃圾桶？

？垃圾桶裡應該要有還原功能？不然就只能開電腦找回來也蠻克難的。

？按照現在的流程，打開再關閉本身就會觸發兩次雲端事件，滿搞笑的

？在本地記錄所有被編輯的檔案？然後回到目錄時逐個備份到雲端。
這個效果你可能非常難驗證有沒有被正確實作（X）

？進行中的記錄沒有被正確刪除，都固定被保留下來了。

？System 資料夾目前仍未備份任何檔案，可能也是備份的時機不明確。

//自訂計分紙的排版介面新增項目沒有對齊效果了，可能是有些細微的設定錯亂了

瀏覽歷史紀錄的情況也會出現虛線框，不過這其實不是很重要就是了

//右上角的截圖沒有跳出選項，只有直接跳出截圖預覽介面的功能，這倒是稍微值得參考，但還是缺少了拍照與瀏覽圖庫功能

？在照片圖庫裡，按上一頁會返回目錄，不應該是這樣，裡面各個介面也都有這種錯誤情況，介面架構樹沒有正確連結？

//在目錄介面明顯的從中間往左滑右滑要能切換計分板介面跟歷史介面

雲端隱私條款的說明？我能夠自己寫內容嗎？

？雲端上傳遊戲記錄的圖片效果尚未完成，難道是第四種欄位？圖片？
計分紙的效果好像也不知道存在雲端的哪裡

//照片計分板的功能，在匯入時檢查項目數量時發生錯誤？可能是因為項目的uuid 寫法不同的關係？

目前有隱藏功能的效果，但只有在背景圖片沒有被項目對應到時才會有此效果，一般則無此預設，做出這個功能其實滿沒用的

//截圖後拼接起來的文字效果跑版了，沒有置中？變成置頂了？

//拉伸的圖片不會自動拉伸其他圖片，變成露出黑底

//預覽截圖的功能變成靠左了，原本是置中
有背景圖的情況難以修復，就接受靠左吧……

//在有背景紙的情況下，截圖功能壞掉了，版面大幅錯亂，但好像跟圖片有關係，或是跟有沒有外框以外的部分有關
計分板畫面以外的部分似乎也沒有正確結合
只有某些情況才會壞掉的樣子？
雖然修好似乎是修好了，但某些情況下不那麼泛用，左邊的項目欄要保有一定程度的寬高，才會支援直式書寫
應該是鎖定項目的最小高度？然後維持比例去調整寬度
縮放對應的放大縮小不是文字而已，包含項目列被鎖定的高，而因為寬自由了，就會變大
現在裡面隨著文字推擠寬度的效果不見了，還好一般計分板的還在（X

？鎖定不會旋轉？但這樣就阻礙了電腦用戶，雖然覺得也不是很重要

釐清計分列有無背景圖的邏輯，程式碼可能要重新整併
結果是改到崩潰，可能放棄這個整併計畫

//刪除計分板時，對應的進行中記錄也會刪除


//在沒有輸入任何值的情況下，點按總分不是添加，而是設定的概念
還是都改成直接輸入結果的概念吧……

//項目類型把列表跟查表交換順序？（因為查表設定複雜許多，而列表跟分項累加的按鈕輸入有類似之處，查表跟自動的核心功能相關）

//支援0個項目（供大量匯入使用（？））

//備份並還原的功能居然壞掉了

//刪除照片的彈窗圖層錯誤
確認儲存照片時的介面按上一頁會直接關閉計分板

？歷史紀錄裡上傳圖片不會備份到雲端，因為不會有更新日期（嘆）
自然就也無法確認上傳下載是否備份覆蓋
雖然改了程式碼，但測試時還是沒反應，至少一鍵備份時有效果就是了
大概是會有效果了

偶爾預覽截圖時會觸發閃退

//更新版本時無法自動根據使用者意圖重新連線，但又會認為使用者想連線？
在這種交錯的狀態下更容易出bug ？
很容易黑屏
修改為每次打開都重置意圖

//有背景的預覽截圖按鈕有點怪，沒有先跳出預覽介面顯示載入中，反而是要等一段時間才跳預覽介面

//有背景的分項累加還沒做好顯示效果。

//新建造的計分板直接進入編輯

//點擊玩家名稱時不應該跳下一項？因為還有可能有很多設定，直接改名字就好

//有沒有背景紙應該在進入頁面給使用者選擇啟用，不用進去再跳訊息
目前改為完全不提醒，按右上角才會知道

//跳出輸入介面時點計分板名稱會同時出現虛擬鍵盤跟App鍵盤

//編輯項目右上角多一個複製效果？
還是單純說把複製的放在原本的下方就好？可能要有一個打勾來切換是否要複製於原處下方，以及設定數量嗎……上限設為5避免太容易被好奇心玩壞
//而且新增或複製完以後也不必關閉新增項目的介面？
既然不會關閉，問題就解決了。大不了按5下

？刪除圖片時不會清除雲端備份，json 檔案理論上已經被更新了？但卻沒有對應檢查消失的圖片uuid

？有背景圖片的縮放後，截圖功能可能會排版錯位，尤其是右邊多出白白的（放大時），縮小時則無此問題

//右邊沒有東西但卻可以左右滑動的bug ，以前的版本沒有這個問題

//有輸入玩家名稱的也要視為有進行中記錄，但名稱本身要怎麼被設計？
先把內建的玩家1、2、3、4排除在歷史紀錄之外，並且給它們對應的ID，避免被儲存進歷史紀錄

//在照片的上下添加遊戲名稱跟玩家計分？

？上傳背景圖也要能四點矩形
不過這個問題的本質是，會是同樣比例的圖形嗎？像素不同的話又能正常運作嗎？
似乎不行。
但自己知道長寬比的話可能就可以

//圖庫左右滑似乎可以切換照片，但要跟拖曳圖片分清楚

//照片上的分數可以再矮一些嗎？
既然框的底色一樣，名字是不是也可以一樣呈現顏色？（注意深色系的渲染），分數也是，但或許不好看。
還有底色淡淡的色系也行

//沒有正確顯示雲端已備份的狀態

//在目錄裡已經置頂的情況下，下滑往左右按雲端跟搜尋

//左滑右滑切換玩家欄的功能（抖），快還要再更快
好像可以用拖曳右上角的切換鈕來實現……按住到哪個玩家下面就是跳到那個玩家？
會不會對大拇指負擔很大（X）
但似乎確實只有大拇指單手操作時能如此高精準度
在輸入介面裡的話會跟上下滑動衝突，但或許也不太有人真的需要上下滑動……只提供左右的話，當停頓在一個計分格輸入框裡3秒鐘以上沒有操作時，就跳出提示手勢操作的示意
怎麼提示使用者？

//呼叫連線似乎也沒用，還是用之前的方案可能比較好，待修改回去。

搜尋後東西又要怎麼下來能被按到（X），極端渴求所有操作都在下半部的設計

//當搜尋框裡有東西時，就不會隨著點擊版面而隱藏搜尋框
此情況下使用者按上一頁就要取消當前的搜尋內容回到原本的目錄

要仔細檢視載入的資料量消耗多少時間

//有背景的計分板看能不能不要那麼強迫使用，而是讓使用者知道可以用就好。但在排版結構明顯不同的計分板中，你變成要製作隱藏的版本……？只有使用者對應點擊時，才會使用到嗎？（但這樣就缺少了範例學習的機會（X））

//有了左右滑動，下一個的功能改為預設向下就好

然後每次切換成向右的時候提示使用者（X）

//總分列也要能支援下一個的功能，但是不能從外面進來，只能從裡面出去

//框選區域裡的文字大小待確認，應該是要預設跟框一樣大（盡可能填滿框）

//有背景紙的計分板卡片怎麼顯示來提示？有架構跟有圖？

//自動按下f()就游標跳到括號內就好，不需要只輸入左半邊（萬一使用者沒有要寫右括號的概念怎麼辦（X））

？歷史紀錄同步至雲端還沒改成非同步

//顯示有沒有備份到雲端其實沒什麼意義，反正一鍵備份時就會知道，不如還沒備份的亮起備份鈕就好（修改日期比最後同步日期還新的）

//一鍵備份後，離線端的顯示仍沒有正確（大概知道問題在哪裏，或許沒問題了）

圖片的索引是怎麼從google drvie 取得的？不太懂，全部的檔名嗎？
連問題本身是什麼都不太懂了（X）

//進行中遊戲是否要被納入計分板裡，不要給使用者看到單獨備份的機會？
還是讓它備份，但不要一鍵下載

//背景功能尚未測試是否都能對準
結論是不能，但很接近了，而且有提供微調的方法

//項目上的標籤都會在截圖時被正確移除嗎？

做個卡森市卡牌版的計分紙（？），既然有人提出來的話（X）

有背景的計分紙，總分那列似乎還是被拉高了，可能是被皇冠撐高高度的關係？
可能還是沒改，但有點麻煩

？刪除進行中記錄仍沒有非同步的感覺

？修改計分板回到目錄時似乎也沒有同步，明明修改日期應該比雲端新了，但也沒有顯示
奇怪的是它被視為同步了，但雲端沒有接受這個覆蓋，難道是雲端沒有update 的日期導致無法比較？
應該要確定有覆蓋雲端的情況才記錄同步成功的時間，失敗的話就不記錄
強迫備份居然也無法取代，令人震驚（X）

Google drive 權限還是要看看能不能寫文字進去，提供給使用者簡介
還沒寫（？）

//用內建計分板匯入新建圖片的計分板時，不應該讀取其檔案名稱，也不應該變成內建計分板

//舊版網格會直接報錯（？）可能就不處理，未來也不會有舊版網格
就不處理吧

//修改網格回到上一頁應該還原原本的四點校正處

網格似乎仍不一定正確顯示右半邊的內容。

//編輯網格時應該要先採納原本的網格，而不是重新設置。

？有些東西似乎總是不會自動備份？甚至上傳也沒有效果？不太確定跟新舊版有沒有關聯，還是跟圖片版有關聯（上傳圖片會更加困難？）
測試後好像會上傳，只是不會更新

？不確定清空垃圾桶的效果是不是三個垃圾桶都清理了，看起來可能是，但這樣不符合想像

//輸入公式時自動往右滑動方便瀏覽

//確認什麼時候才會覆蓋indexedDB 的內建計分板
indexedDB 版本號增加的時候

//雲端變成上傳檔案了，沒有創造資料夾？

？設定記錄檔被重複存放了？但修改日期怎麼都一樣

？進行中遊戲似乎沒有正確地被備份或刪除

？歷史紀錄檔案沒有隨著照片更新日期？至少UI上沒有，但確實有上傳

//某方面來想，如果會自動備份，那其實就挽救不了什麼嗎（？），修改的時候還是會一起壞掉
除非計分板修改一律要手動上傳？
但想想一般使用者要用到壞到難以復原的程度是很困難的？（只有舊版網格會崩潰）

//分項累加乘積的單位如果預設分數在後面，單位要怎麼顯示？固定顯示後者好嗎（嘆）
即使沒有單位，仍然顯示了「個」，這樣不妥，任何單位都變成個了，錯用到一般倍率的單位了
新增一個結果單位的位置，使用數字倍率的單位

//退出時就要有個直接捨棄的按鈕（在不好按的地方），左上角印「作廢」？捨棄

//確認刪除記錄的彈窗點外圍也要退出

//在遊戲進行中點相簿就自動跳轉開相機或上傳？只能說也無不可，或者說要把拍照鈕獨立出來。
連拍功能，拍完一張應該繼續拍，直到使用者自行退出拍照畫面

//項目最下面多一個新增的按鈕，不會被納入截圖裡，點按可以新增一個空白項目

//沒有搜尋結果就直接讓他點按新建，然後名稱預設它搜尋的名稱（但提供繼續輸入）

//輸入名稱時有可能視窗跳掉，觸發條件未知
只有內建ID改名時會觸發此問題，留在原地的問題？

//簡潔版的截圖能不能當作分項累加的內容不存在？

//玩家id 短些，地點也要有id，然後實作一個記錄表（？），每次被關聯就往前推一格，後面的人推5格（？），每個人記錄20個人，5個地點

//下一項會正確往下，但卻不會正確往右（編輯名稱時）

//左右滑動時也要輸入，而非捨棄

//大概知道排序問題怎麼解決了，新人加入的位置應該不能超過該場最前面的人
但該場最前面的人又該獲得什麼buff 嗎？要如何兼顧「最近優先」與「最頻繁優先」兩種排序？

//要做一個常數表，不然最後我也不知道儲存了多少記錄，上限多少？

//照片加計分的，要用均衡排列比較好看，32而非41，33而非42

//將圖片網格錯誤的圖更正或刪除網格記錄變成原版的

//合作遊戲的獎盃如何設定？預設同贏，但有人按強制落敗就同敗

製作有角色的計分板範例

找幾款遊戲計分有自動化的，有計分紙的來錄影

//簽名補東東（？）

//放大後的截圖，就沒辦法正確地填入原本的背景圖裡？問題是格子本身也沒有對齊？
簡潔版的上面不知何故有條白線
有背景的簡潔模式截圖也不需要印出皇冠

//玩家姓名歷史紀錄UI可再優化

//輸入玩家姓名好像又不會儲存了？

//從歷史紀錄還原計分板的效果

//直接從搜尋新建時，預設項目數量為0，還要可以設定玩家數量？
還要直接鎖定到玩家跳出編輯介面？

//其實想錯了，應該是改成立刻建立一個0項目的計分板，然後跳到進入介面，反正遊戲名稱也可以裡面再改（？）

//添加的牆壁層數應該就是該頁所在的深度
總之對手機端用處有限，還是得等到V4大改（X）

//更新一下雲端介面的描述，是讀取/改動，不只是新增刪除

[[萬用桌遊計分板V4發想]]
