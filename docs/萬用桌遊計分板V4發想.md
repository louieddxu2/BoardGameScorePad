//尋找別人提供bgstats 的資料結構來製作轉換檔案
匯入bgg 然後自動建立所有無內容的計分板，方便未來編輯
這種無內容的計分板是不是應該獨立存放，不要跟有內容的混在一起（？），只要能搜尋到就好

自動根據遊戲
//玩家，人數，星期，時間，預測地點
要儲存許多投票列表吧，地點有一些人跟時間；人有一些人跟時間跟地點；時間也要有人跟地點嗎（X）相對來說比較不明所以的記錄資料

資料庫架構不再使用constants.ts來存放，改成json 檔減少編譯時的阻礙？
似乎未必需要，只要安裝時動態載入就可以了？或是點按時再載入也可以……先有目錄就好
好像有改成動態載入了

可以連結github 讓我想上傳就上傳的功能（？）
//點擊雲端五次開啟開發者模式之類的
結果可以直接連到雲端試算表

//記錄儲存空間上限，有估算了

//照片有其他資訊記錄可能會在上傳時遺失？旋轉、拍照日期，但既然都要壓縮，好像沒什麼好計較的。

//拍照的玩家名稱可以被換成圖案（？），或是只要是系統id就換成圖案
匿名功能，在玩家那裡設定，如果匿名就是截圖都不會出現

要根據擴充跟隨機設置來決定項目的功能，但這其實蠻麻煩的。
不如直接給不同的計分板（X），但這對如起司那種遊戲就很不禁用。

公共雲端的功能等有人真的寄信給我再說（X）
//但這樣就能提供多國語系了……？

雲端認證讓登入頁面更安全的感覺
目前是有寫好介紹文了

首頁只顯示我的遊戲庫跟內建遊戲庫各上限20筆？反正有釘選功能
內建遊戲庫還是50筆吧，對於完全無知的人還是有直觀的操作空間。

簡易模式（？）拍照、地點、玩家、分數單純分項累加的功能，但退幾步來說，你能做得比bgstats 還要更簡單嗎？
//當項目不到5項時，下方的介面有空間可用，可以用來放截圖、拍照等等的快捷按鈕？

//極端一點將bgg 頁面匯入App就自動建立計分板？但也只能建立空白的，只差在有bgg id而已（X），好像沒什麼意義
其實還有一些資訊，但有些都有時效性，盡量取那些比較穩定的來記錄？
已經能匯入並記錄很多東西了。

自動填入的效果在6小時內有記錄的情況，前2格永遠顯示最近使用的，後面才是其他推薦
目前記錄遊戲的方式是看遊戲名稱完全一樣，這在未來取用時可能會有問題

完全沒有項目欄的計分紙無法使用（X），因為沒有辦法設定項目，要為這個情況獨立設計，在編輯模式下項目鎖定寬度

有預設玩家顏色的也可能無法製作（嘆），處處受限，因為你的姓名框也要能夠設定框選區域才行

//四點定位旋轉圖片時四個點會跑到很遠的地方，不符合預期

//截圖圖片靠左的問題始終無法解決（X）

角色功能，自動取前幾欄用「只顯示標籤」的內容合併為角色？

點左上角可以設定玩家姓名的框選區域
//以及這個遊戲的玩家顏色有哪些

如果上次更新時間在5分鐘以前，就自動跳到玩家姓名欄；或是透過搜尋進入時也是

//搜尋東西時用空白鍵或逗號區隔多項搜尋
搜尋時按右下角enter 應關閉虛擬鍵盤
想想搜尋時新建計分板的功能可以極端地直接放在最下面？跟可能的廣告位置衝突（X）
跟bgg 記錄結合的話就是再跳出選單

到頭來你在滿足誰的需要？
你自己最初的需要很簡單，你要有一個比寫計分紙計算得更快、更方便的軟體，在所有情況下它至少要跟計算機一樣快。
再者的需求，是想記錄、分析自己的遊戲歷程的玩家。
這需要轉移資料的功能，並且要能高速預測。

再者的需求，是想推薦遊戲的玩家。
你的推薦算法是已經有了，但它需要歷史數據作為支持，或是bgg 的物件也可以參與投票（？）。

？結果匯入匯出好像已經完成了，剩下細節優化（X）

//要找個自己比較方便的對照bgg id 的方法啊……不然自己手動也很累

//模糊搜尋已完成（？）

//匯入時連連看的介面與功能
還遠遠沒有完成（？），連完全一樣名稱時的狀態都還沒處理完，計分板要如何被填入內容，跟遊戲記錄對應。

匯入資料的介面裡的按鈕目前還沒什麼用，不知道要不要刪掉
要用來當作匯入bgg 記錄的按鈕嗎（嘆）

//匯入內建計分板時，就直接擷取id作為別名吧？好像比我自己填寫多個欄位還要更快（X）
好像已實現

匯出沒有bgg id 的東西給bgstats會怎麼樣？

原本想添加collection 進去，但還是太困難了，介面很難沿用？數值輸入、文字輸入、網址輸入與超連結、寬度由項目內容來決定、渲染，某種意義上的不可共性（X）

輸入地點時一樣可以跳出上方的選單，不限定要按到右邊箭頭？且右邊區域可以再大一些

//橫版照片功能不見了

//是否自我限縮到不考慮持有者？評分等等的，讓推薦的邏輯盡可能簡單
計分，然後把記錄匯給bgstats ，使用者有的遊戲應該在bgg 記錄就好
斷點要決定好……
能匯入，在沒有新遊戲的情況下能記錄，然後能匯出
多添加一點的話是能新增一些基礎的新遊戲。
//搜尋到的遊戲可以跳出縮圖……但僅限前5筆資料
放棄縮圖吧

歷史紀錄裡輸入地點時也要有下拉式選單

歷史紀錄已還原不需要跳提示訊息

//列表選單還是沒有亮起當前選項（嘆）

//僅顯示分項在一般情況下不應該隨著數量3組以上字體變小，那是有背景紙時才需要的功能。

//重疊選項的翻譯，列表或重疊

框選區域還可以選置哪個方向（置右之類的），預覽文字根據寬度來決定預覽幾個數，有些情況可能只需要一位數或二位數，看著三位數的大小會誤判

//點按右下角開始時，如果沒有搜尋內容，拒絕開始時會直接跳離此介面狀態，應該要維持會更易懂

//0項目的計分板到底要怎麼處理？理論上是不必保留，但核心精神是使用者必須盡可能仰賴儲存的記錄
但同時又有釘選的問題。
要設定哪些東西，才被視為要保留此計分板（而非直接刪除）
就算是預設的計分規則，似乎也不用寫在計分板裡？也可以寫在savedgame 裡吧

//考慮看看總分列要不要往上移動，雖然這種修改可能會很複雜
結果用了取巧的方式，有了介面也可以設定按鈕（？）

？背景跑統計時，能不能使用其他功能？但還是乾脆迴避這個問題更好
其實可以，但不建議

//不知道為什麼變成只顯示我的計分板1筆了，雖然解決了，但對於顯示共2筆不太滿意

//搜尋推薦前5筆重複的問題

//簡易計分板也不是完全沒意義就是了……
在搜尋框裡提供釘選的按鈕，好像可以，按下時就直接建立，而取消釘選時也會檢查要不要直接刪除

//縮放時下方的介面不能跑版

//某方面來說你也不太敢測試，10MB真的很大（X）
應該是渲染的問題，改成右邊只顯示20筆其實也沒差，不會有人不使用搜尋的方式去尋找（吧）

清空資料庫時保留bgg？

//小數點輸入似乎只有分項累加下支援，怪怪的，應該要全部都支援才對

新遊戲本身也是一種推薦基礎（只要是新遊戲，就按照某個記錄來推薦）

//結果添加了一些小工具……不是完全沒意義就是了

//拍照是改為單張就進入預覽的效果
畫面新增項目那列最右邊有個小工具按鈕，按了跳出彈窗？

//彈窗刪除的圖層又錯了

3.5版發佈前還要把內建模板都補上bggId 吧，沒必要再讓別人連連看。
不過這實際上會發生什麼事情啊？如果找不到bgg 記錄的話，好像不知道會怎樣。

要不要內建一點點bgg記錄就再說，會麻煩不少，不如叫使用者自己匯入（X）
先把計分板遊戲加入自己的bgg ，然後下載下來自己打別名，然後再匯入自動配對，最後就得到有bggid 的版本了
//還是要做一點吧，這樣才有別名系統（結果讀資料還要去讀別名？）
搜尋時括號顯示被搜尋到的別名

//為什麼有些遊戲會是寫ID

//推薦何時猜中要加0.5

//有bgg id 的東西不需要被連連看

//偏好顏色應該是有被設定的才會有
而且也有連帶的問題，如果兩個人推薦的顏色一樣會怎麼樣？
先被推薦的人優先選色（？）

//一個很難統一的saved game 跟計分板名稱不同的bug ，長遠來看他們只能透過寫入相同的bggid 來統一？

//不知何故拍照又變成3欄了，有點醜陋（X）而且是刻意換行？

//重複匯入會怎樣？匯出時也不需要考慮有bgstats 記錄的內容？
好像已經透過歷史紀錄uuid 完全同步解決這個問題了

隱藏介面裡照片應該要用其對應的歷史紀錄或計分板名稱來命名才好搜尋

短期記憶的權重不應該被重置？
重置權重跳出的提醒視窗應該要是我們UI的版本

階級爬升的機制，次數確實也值得考慮一下，新人不應該插在50%，應該要插在最前面使用次數1的人之前
還是說每次都不是減半爬梯而已，是減半跟次數相同的人之前取小，這樣似乎更加合理些。
好吧也不能說是次數相同，第一個次數小於等於它的人？
只是有點不太確定，這樣弄完的結果會是什麼，次數高的人每次都可以直接跳回前面（即使很久沒出現），而次數低的人要慢慢減半爬？但也不會爬得很慢就是了
玩家的情況則是依然要維持原排序，然後每個人也考慮從前一個人排到的位置往後的，第一個次數小於等於自己的人之前

釐清一些介面的i18n 吧，幾乎像是在規劃單元測試（X），有些東西是每次發布前都需要處理的

根本沒搜尋到東西時，為什麼還會有選項跳出來？
蒸氣帝國跳出了手機帝國？似乎也不是說不通，但為什麼沒顯示中文詞彙？應該是因為有些閾值的設定。

//列表選單選了以後不用自動往下？自動往右時是方便的，但當常態固定向下後，自動往下就有點干擾了

//拍照好像都上下顛倒了，當橫向拍照時（嘆）

既然能連試算表，那也可以直接把json 寫在試算表的一欄裡吧？

？雙語化好像快靠AI完成了